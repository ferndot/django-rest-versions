============
Installation
============

At the command line::

    $ easy_install django-rest-versions

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-versions
    $ pip install django-rest-versions
